import React from "react";
import ComputersCanvas from "./components/ComputersCanvas"; // Adjust the import path if necessary

const TestComputersCanvas = () => {
  return (
    <div>
      <ComputersCanvas />
    </div>
  );
};

export default TestComputersCanvas;
